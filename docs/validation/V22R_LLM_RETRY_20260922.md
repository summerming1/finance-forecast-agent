# V2.2-R quota replacement and supplemental validation — 2026-09-22

This is the user-requested continuation of the [first supplemental audit](V22R_WINDOWS_SUPPLEMENT_20260922.md), not a replacement of its red evidence. Only validation, a one-line prompt clarification and test isolation changed. No new Controller, Queue, evaluator, Memory, task type or V3 implementation was introduced.

## Git, environment and input

- Branch: `feat/mission-research-v2`; initial local/remote SHA: `5040c6126373d436d1cc8db90f1318e3c62b0229`. Fetch found no newer remote commit. No reset/stash/clean/force push.
- Original requested R6 audit base remains `625283636cef4faa080f8521683f81491611b5ff`; R6 code was already committed. The previous supplemental commits were retained and reviewed, not reapplied.
- Windows 11 build 26200, Python 3.13.3, Intel i5-12500 / UHD 770; no CUDA GPU. `OMP_NUM_THREADS=1`, `MKL_NUM_THREADS=1`.
- Initial tracked tree clean; ten pre-existing untracked test/project directories retained. New raw runs stay under ignored `validation/`; restored historical assets stay in their existing ignored project paths.
- Frozen real SPY: `inputs/spy_chart_2010_2025.json`, SHA256 `0bb0896126adb0393f34ae09b90487cb501b2c6aa681a66d2a8f02348669036b`; semantic identity `589e8d9aae8433cb45e52a67cda15961242d55743d020aa7b810d6567ebce9ac`. 4002 rows, 2010-02-03 through 2025-12-30. Always `historical_development_only` / `forecast_only`.
- Raw command/evidence root: `validation/llm_retry_20260922/`. Commands below use `.venv/Scripts/python.exe` as `python`; manual browser durations and uninstrumented operations are null, not inferred from file timestamps.

## Quota and model policy

The original `qwen3.8-27b` probe returned **HTTP 403, `AllocationQuota.FreeTierOnly`** (0.2667s). Replacement `qwen3.7-flash-2026-07-15` returned **HTTP 200**, valid `{"ok":true}`, 252 tokens (3.7348s). These are actual provider observations, not an attribution inferred from a timeout.

The user-authorized chain is `qwen3.7-flash-2026-07-15` → `qwen3.8-flash` → `deepseek-v4-flash-0731` → `deepseek-v4.1-flash` → `qwen3.7-flash`. The validation driver rotates only on explicit quota exhaustion, preserves the failed arm and charged resources, and creates a fresh arm/model contract. Invalid proposals are not retried with different models until a winner appears. `.env` is unchanged; test processes override `OPENAI_MODEL`. No credentials or raw authorization headers are committed.

## Minimal fixes and preserved failures

### Strict response envelope

Pre-fix live evidence: One-shot completed (12 candidates, 60 fits, one call, 135.96s); Adaptive then returned a schema-name wrapper and failed strict compilation (one accepted candidate, 16 fits, two calls, 97.86s). Both records remain under `validation/lr22`; these exploratory runs are not pooled into the frozen post-fix comparison.

Commit `02acafe0e137f4c5b72be915906dfd5d796e6582` adds one prompt rule requiring only top-level `hypotheses`. It does not unwrap responses or weaken validation. Two tests preserve wrapper rejection and assert the instruction. `python -m pytest -q tests/test_focused_supplemental_validation.py -k schema_name`: exit 1, 1 failed / 2 passed / 28 deselected, 2.51s before repair. Entire file after repair: exit 0, **31 passed, 4.39s**. Scoped Ruff/compile pass. Full-run cumulative focused/queue/memory subset: **231 passed / 1 failed / 2 skipped**, 234 cases, 3198.962s summed testcase time; failure is the native symlink setup privilege, not relaxed or skipped.

### Historical workbench test isolation (`92a81895824ff29ab09981dcf3e8e5ca335e9c9c`)

The previous test implicitly assumed the user's installed reproduction plan was unapproved. After restoring original historical assets, an actually approved plan correctly enabled the UI, so the test's unconditional disabled assertion failed. The test now creates a marked simulation-only card/plan in its own temporary project and separately tests approved and unapproved navigation. It verifies the saved plan bytes remain unchanged. No production UI or approval logic changed.

Original restored-history targeted run: exit 1, 7 passed / 2 failed, 5.73s (the other failure was PDF extraction identity). The first test-fixture setup missed `source_path`: 10 passed / 2 failed, 10.03s; retained. Corrected workbench file: exit 0, **12 passed, 10.20s**. Combined affected historical/workbench files under the matching PDF environment: exit 0, **27 passed, 11.71s**. A mistyped nonexistent test filename also produced a no-tests exit 1 before the corrected invocation; it is not a product failure.

## Actual commands and new results

| Command (relative to repository) | Exit / measured result | Evidence and scope |
|---|---|---|
| `python validation/llm_retry_20260922/probe.py qwen3.8-27b` | HTTP 403; 0.2667s | Explicit exhausted allocation, not successful research |
| `python validation/llm_retry_20260922/probe.py qwen3.7-flash-2026-07-15` | HTTP 200; 3.7348s | Actual small provider response |
| `python validation/llm_retry_20260922/live_replay.py live` | 0; completed, 2 rounds, 20 fits, 2 HTTP requests | New actual provider calls / frozen historical SPY; no hidden sentinel in actual request body |
| `python validation/llm_retry_20260922/live_replay.py replay` | 0; completed, 2 rounds, 20 fits, 0 requests / 0 blocked attempts | New process, all three key variables cleared after env loading; socket, requests and provider construction denied; prompts, hypotheses, configs, fold metrics and every prediction equal |
| `python validation/llm_retry_20260922/live_memory.py` | 0; cold 131.2489s, warm 120.3609s | Real provider, same task/budget, immutable snapshot; details below |
| `python scripts/run_focused_spy_campaign.py --project-dir validation/sm22 --raw-spy-json inputs/spy_chart_2010_2025.json --advisor-mode deterministic --rounds 3 --candidates-per-round 2 --max-fit-calls 40` | 0; completed, 20 fits | 4002 real rows / expected period; confirmation `not_run_historical_data_exposed`; not independent evidence |
| `python validation/llm_retry_20260922/audit_smokes.py` | 0; 40 PredictionArtifacts recomputed | New deterministic smoke, live, replay, cold, warm: 8 artifacts each; MAE/RMSE/direction and every fold |
| `python -m pytest -q --basetemp validation/fr22 --junitxml=validation/llm_retry_20260922/full.xml` | 1; **391 passed / 3 failed / 3 skipped**, 3245.62s | Full run loaded the old workbench test at collection; not a final all-green claim |
| `python -m pytest -q tests/test_p1_real_validation_artifacts.py tests/test_native_catalog_builder.py tests/test_multi_benchmark_suite.py tests/test_streamlit_workbench_app.py --basetemp validation/histfinal22b --junitxml=validation/llm_retry_20260922/historical_final_corrected.xml` | 0; **27 passed**, 11.71s | Separate process `PYTHONPATH=validation/pdf614;src`, restored original assets; no native training |
| `python validation/llm_retry_20260922/legacy_replay_offline.py` | 0; **4 passed**, 1.31s; zero network attempts | Same isolated parser; strict offline historical fixture, never regenerated |

Live/replay and smoke wall durations not captured by their helper remain null. Provider per-call durations are available in records; they are not substituted for campaign wall time.

### Full-repository failures, individually

| Nodeid | First failure / classification | Relationship and recovery |
|---|---|---|
| `tests/test_focused_r4_trust.py::test_model_bundle_tamper_rejected_before_load[symlink]` | WinError 1314 creating link; `BLOCKED_ENV` | Existing Windows privilege limitation before deserialization guard; do not convert to PASS. Linux evidence must be tied to its own SHA. |
| `tests/test_p1_real_validation_artifacts.py::test_dlinear_strict_live_card_replays_without_an_api_call` | Missing matching prompt-hash fixture after PDF extraction; `BLOCKED_ENV` | Same original PDF bytes, main pypdf 6.19.0 yields text hash `304b39076343587b`; historical pypdf 6.14.2 yields `c611ce7852956016`. Isolated 6.14.2 passes all four tests strictly offline. No production hash relaxation or fixture rewrite. |
| `tests/test_streamlit_workbench_app.py::test_existing_methodcard_moves_through_review_and_setup` | Expected disabled=True, actual False; FAIL (test isolation defect) | Existing local approved plan correctly changes UI. New approved/unapproved isolated cases pass; production permission remains unchanged. Full suite had already collected the old case; targeted result does not retroactively erase it. |

### Historical assets recovered, not regenerated

Original local checkout `D:/AI Agent/finance_forecast_agent` at `12fa13ac58c358db95a088d1fc6316467874d23e` supplied only missing PDF, curated MethodCard and multi-benchmark report. SHA256 respectively: `97abddd1821cc72942c8d7ddde7e99466bb91f1bddc37c2b54e0e97be7b5be1b`, `fd148c023e760cd068c1a777437e0c09a7402a00eac39f034ea0db28cb154bc1`, `defed153676654f78d5dbe8951643f98f2342d4cff297b981314f266537b9f2c`. No existing target was overwritten. Official fixed-commit FEDformer / ETSformer / FiLM archives were restored with path and hash checks; commit/archive/entrypoint hashes are in `restored_sources.json`. The post-restoration native catalog check passed 8 tests (2.29s). Downloaded native code was not executed as training.

`python -m pip install --no-deps --target validation/pdf614 pypdf==6.14.2` created an isolated compatibility path. The main environment is still pypdf 6.19.0; its historical strict Replay remains explicitly incompatible. Hour-scale/GPU/original native training: `NOT_RUN_COST`. Restoring an old report is not rerunning its financial experiments.

## New real Live Memory ablation

Frozen snapshot SHA256 `00bf9021fa7b0919ff58087da8a6fcdb3580ca3c23a9f15dcf80c19eebc8831c`, containing two accepted experiments from the new live smoke. Both arms use the same two-round / one-candidate / 20-fit budget and model, separate campaign state under the existing runtime implementation, and the unchanged evaluator.

| Arm | Visible prior | Repeated prior configs | Unique candidates | Charged fits | Invalid proposals | LLM calls | Best MAE | Seconds |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| cold | 0 | 1 | 2 | 20 | 0 | 2 | 0.005033119753487377 | 131.2489 |
| frozen warm | 2 | 0 | 2 | 20 | 0 | 2 | 0.0050229874405042455 | 120.3609 |

Provider cost and measured human minutes are null. This tiny historical-development comparison is not independent generalization, statistical proof or a causal LLM-superiority claim.

## Browser and carried-forward engineering evidence

An actual in-app Chromium session reopened the persisted historical-SPY campaign `persistent-e59f422d6bee56d15c326054` in the existing runtime DB. Candidate A `r1_c1_61e41d11` and B `r1_c2_ad30eb4d` each matched the selected value, JSON detail and artifact reference after rendering settled. Reload and a new tab/new Streamlit session restored the campaign; before and after remained **8 attempts / 20 charged fits**. This probe did not create a new browser context or retrain a new customer campaign. The browser skill was used to inspect actual rendered state; AppTest was not substituted for it.

The prior report's approve/reject, complete historical-SPY crash/resume, simulated CSV client and fresh-process trusted ModelBundle remain explicitly **carried-forward evidence**; they were not all manually re-executed in this small browser restoration probe. The new cumulative suite separately exercises their automated coverage. No actual BYO customer, eligible unexposed confirmation input or macOS environment appeared. Portable trust migration remains `NOT_IMPLEMENTED`; copying SQLite is not a migration mechanism.

In addition, the current-source full dedicated Chromium test was rerun: `FFA_BROWSER_E2E=1 FFA_BROWSER_ARTIFACTS=validation/browserretry22 python -m pytest -q tests/browser/test_research_workspace.py --basetemp validation/br22 --junitxml=validation/llm_retry_20260922/browser_final.xml`, exit **0**, **1 passed / 137.25s**, Chromium **153.0.8010.12**. This separately validates queued training, both completed research candidate values/details, fresh browser context, reload, package hashes, explicit bundle refit/download, no extra research fits, supported Chinese/unsupported scope handling, history reopening and the simulated Parquet client. Synthetic input SHA256 `e639dcf25166f3dcba0a9c01945a516e7d72a891b0023c62bb17f542a88e42d5`; report/screenshots/DB backup remain under `validation/browserretry22/`. This is actual Chromium engineering E2E, not a real customer or financial confirmation.

## Final matrix / regression / Git checkpoint

`python validation/llm_retry_20260922/audit_browser_delivery.py` returned 0 in a separate verification invocation: 24 package file hashes, eight PredictionArtifacts with fold/aggregate metric recomputation, and Manifest candidate identity/seed/features/requested and effective parameters matched. Downloaded bundle metadata/model bytes match the original registered directory; a fresh Python process and the parent produced exactly equal feature-only predictions using the **original** runtime DB. No refit was added and no downloaded path was silently registered. ResearchPackage SHA256 `3af255531d86f70c1b5e60c6bc9ab86cd7da858bd91098c231833010a6112158`; ModelBundle ZIP SHA256 `13f7cbf38037d308671d500664fd8fc42fedd8015522d92fc7f1068d1b98bf89`. This remains simulation-only serialization/delivery evidence, not held-out performance. Owned test servers exited; user services were not stopped.

Final cumulative command (PowerShell expands the file patterns before pytest):

```powershell
$env:OMP_NUM_THREADS='1'
$env:MKL_NUM_THREADS='1'
$tests = @(Get-ChildItem tests/test_focused*.py,tests/test_task_queue*.py,tests/test_*memory*.py | Select-Object -ExpandProperty FullName -Unique)
.\.venv\Scripts\python.exe -m pytest -q @tests --basetemp validation/finalfocus22 --junitxml=validation/llm_retry_20260922/final_focused.xml
```

Exit **1**: **231 passed / 1 failed / 2 skipped**, **2290.47s**. The sole failure is WinError 1314 symlink setup. Skips are the existing POSIX process-group probe and unavailable native symlink permission; neither is a PASS. The full suite's third skip was the opt-in browser test, independently run and passed above.

| Stage-specific file in the cumulative command | Passed / failed / skipped | Summed testcase seconds |
|---|---|---:|
| `test_focused_r0_status.py` | 2 / 0 / 0 | 0.002 |
| `test_focused_r1_contracts.py` | 19 / 0 / 0 | 1.088 |
| `test_focused_r2_runtime.py` | 17 / 0 / 0 | 422.077 |
| `test_focused_r3_actions.py` | 14 / 0 / 0 | 161.072 |
| `test_focused_r4_trust.py` | 31 / 1 / 0 | 66.365 |
| `test_focused_r5_benchmark.py` | 19 / 0 / 0 | 711.787 |
| `test_focused_r6_workspace.py` | 23 / 0 / 1 | 279.097 |
| `test_focused_supplemental_validation.py` | 31 / 0 / 0 | 1.738 |

These rows are subsets of one process (exit 1 overall), not invented separate successful commands. Other PR-1–PR-6/shared queue/Memory tests make up the remaining count. Engineering inputs are simulation-only unless a real-SPY/live run is explicitly named. Production source and test tree were unchanged during this final run; later validation documentation is not new training code. Final scoped Ruff (focused source/tests/browser, shared runtime/adapters/delivery scripts, page, workbench test), compileall (source/page/scripts/workbench test) and `git diff --check` passed; logs are `ruff_final.log` and `compile_final.log`.

After synchronizing current-status documents, `python -m pytest -q tests/test_focused_r0_status.py tests/test_focused_supplemental_validation.py tests/test_streamlit_workbench_app.py --basetemp validation/last22 --junitxml=validation/llm_retry_20260922/final_changed_files.xml` returned **0**, **45 passed / 13.19s**. Both JSON documents parse and their cross-checked 37 attempts / 1660 fits / 78 HTTP / 1,180,254 known-token aggregates agree. These are overlapping regression checks, not extra independent scientific observations.

## Completed replacement-model matrix

The frozen 3 windows (2010/2013/2016 starts) × 3 search seeds (17/42/91) × 4 strategies was fully attempted. Every arm uses the original `run_benchmark_arm` / `FocusedResearchController`, candidate budget 12, estimator seed 42, max 60 fits, cold Memory and the same 41-config catalog. Current-source Random/TPE were rerun rather than borrowing old-source scores. Source signature across every arm: `c738ad775168781299f7507eaa2a4d9adaa00ca86d367995bb60100cc36fab2e`.

Commands:

```text
OPENAI_MODEL=qwen3.7-flash-2026-07-15 python scripts/run_research_value_benchmark.py --raw-spy-json inputs/spy_chart_2010_2025.json --candidate-count 12 --startup-trials 4 --seeds 17 42 91 --windows 2010-01-01 2013-01-01 2016-01-01 --arms random tpe --llm-mode live --fixture-dir validation/ctrl22/fixtures --out validation/ctrl22/controls.json
python validation/llm_retry_20260922/run_retry.py
python validation/llm_retry_20260922/audit_retry.py
python validation/llm_retry_20260922/audit_controls.py
python validation/llm_retry_20260922/explain_failed.py
python validation/llm_retry_20260922/cross_audit.py
```

The control command returned **0**; the completed live driver returned **1** with `all_18_live_arms_attempted`. Audit/recompilation commands returned **0**. The first post-fix non-quota failure was inspected, preserved, then the driver continued the remaining frozen arms; it was not retried until successful. The validation-only orchestration does not generate/repair model proposals or replace the existing execution stack.

| Strategy | Completed / terminal arms | Extra quota-failed attempts | Sum of arm-local unique candidates | Charged fits | Invalid proposals | Logical LLM calls | Sum arm wall seconds | Completed-only median relative MAE improvement |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Random | 9 / 9 | 0 | 108 | 540 | 0 | 0 | 1361.05 | 0.370084% |
| real Optuna TPE | 9 / 9 | 0 | 108 | 540 | 0 | 0 | 1367.26 | 0.129354% |
| One-shot live | 3 / 9 | 0 | 36 | 252 | 6 | 9 | 1352.58 | 0.370084% (only 3 successes; selection-biased subset) |
| Adaptive live | 0 / 9 | 1 | 52 | 328 | 8 | 64 | 5034.95 | null |

TPE performed **36 startup / 72 model-based decisions**, 108 arm-local unique accepted configurations summed across nine arms (not 108 globally distinct configurations), zero duplicate proposals and zero failed fit attempts. This is a finite categorical catalog, not arbitrary continuous hyperparameter optimization. All strategies share the same baseline and actual target rows for each window. Configuration identity and execution identity remain separate.

One-shot made exactly one logical provider call per frozen arm and never replanned from intermediate results. The Adaptive audit checked the real accepted results, StructuredFeedback and remaining budget in every saved prompt. **71 returned prompts/fixtures**, **526 PredictionArtifacts** and **1356 raw artifact hashes** passed independent checks, including all folds. Cross-audit additionally matched actual target signatures and baseline config/metrics across controls and live arms. Audit PASS does not turn failed arms into completed arms.

Quota handling was genuinely exercised: `qwen3.7-flash-2026-07-15` exhausted its allocation at the last Adaptive arm (two HTTP 403 `AllocationQuota.FreeTierOnly` attempts). Its 12 baseline fits remain charged. The driver restarted that arm under a new project/model contract with `qwen3.8-flash`, which returned 11 successful responses and accepted nine candidates (48 charged fits) before the final request exhausted normal read-timeout retries. One earlier timeout recovered. The replacement arm ends FAIL, not PASS. No further quota exhaustion occurred, so the remaining three user-specified models were not consumed merely to evade a substantive failure.

Totals for this **new matrix only**, not pooled with earlier evidence: 37 execution attempts for 36 logical arms, 1660 charged fits, 304 accepted candidate executions, zero failed fit attempts, 73 logical provider calls, **78 HTTP attempts / 71 HTTP 200 / 2 quota 403 / 5 transport exceptions** (two recovered ProxyErrors and three ReadTimeouts), **1,180,254 known returned tokens**. Failed/retried-call token usage is unknown; provider cost and actual human-operation minutes are null. Failed fit count zero does not mean provider/research success.

The 14 strict proposal failures were independently reproduced without new training/provider calls: six invalid simplifications, seven proposals outside the frozen catalog/estimator-seed contract, and one unsupported ablation component. These are not quota errors and were not auto-rotated away. One terminal Adaptive failure is transport timeout; one additional superseded attempt is explicit quota failure. The JSON report includes per-arm MAE, same-window relative improvement, fold stability, unique/failed/charged fits, calls, cost, wall time, contracts and source identity: [derived matrix](V22R_LLM_RETRY_20260922.json).

**Research value verdict: FAIL / unproven.** Three completed One-shot plans demonstrate feasible execution, not consistent quality. Adaptive did not complete a single frozen arm; its partial best-so-far scores cannot establish value or fit-efficiency. Small live Memory evidence is reported separately. Overlapping windows are dependent, search seeds are not provider random seeds, and the last Adaptive arm changed model. No single-model causal algorithm comparison, pooled significance or profitability claim is made.

## Git checkpoint and publication boundary

Initial SHA `5040c6126373d436d1cc8db90f1318e3c62b0229`; production repair `02acafe0e137f4c5b72be915906dfd5d796e6582`; test-only repair `92a81895824ff29ab09981dcf3e8e5ca335e9c9c`. Final reporting commit is identified in the handoff/Git log rather than embedding a self-referential hash. Both fixes were committed separately after their targeted and cumulative evidence. The latest fetch still showed origin at the initial SHA. New-production-change publication confirmation is outstanding; no new push or same-SHA Linux CI is claimed. Prior pushes/CI remain historical evidence. The final handoff records exact HEAD and working-tree status.

## Stage boundaries and explicit questions

| Stage | Verdict | Evidence and remaining boundary |
|---|---|---|
| R0 | PASS | Status audit, retained history, new report distinguishes API availability from research acceptance; no R6 fully-accepted claim. |
| R1 | PASS | 19 contract cases + 31 supplemental cases, actual replacement-model Live→strict offline Replay; production prompt repair `02acafe`. |
| R2 | PASS | 17 current runtime cases plus shared queue/persistence coverage; previous real-SPY Windows process-tree crash/resume evidence retained, not newly labelled as a run on this SHA. |
| R3 | PASS | 14 action cases, real cold/warm frozen Memory run; previous real-page approve/reject evidence retained. No generalization/superiority claim. |
| R4 | BLOCKED_NO_ELIGIBLE_DATA | 31 trust cases pass, one native symlink setup BLOCKED_ENV; new downloaded-bundle/original-registry fresh process equal; genuine confirmation still unavailable. |
| R5 | FAIL | All four policies use the same original Controller/compiler/evaluator; real replacement-model matrix still contains substantive failed proposals. A small smoke and working API do not close the value gate. |
| R6 | BLOCKED_NO_REAL_USER | 23 workspace cases pass / one symlink skip, new real Chromium E2E and package/bundle verification pass; all BYO clients remain simulated. |

Stage counts refer to the exact commands and summed testcase times above; they are not additive independent scientific trials. No other production bug was repaired, no failed test deleted, no synthetic customer or historical-SPY blind confirmation invented.

| Requested answer | Current evidence |
|---|---|
| Real Live LLM retested? | Yes; original quota 403 independently confirmed; replacement-model smoke and full frozen matrix actually call Bailian. |
| Strict offline Replay? | Yes; new process clears credentials and blocks network/provider entry, zero attempted requests, exact outputs; historical PDF replay also passes only with its matching extraction environment. |
| R6 real browser passed? | Yes, new current-source Chromium E2E 1/1; separate real-SPY read-only in-app restoration also observed. |
| Candidate switching adds fits? | No in both measured flows; real-SPY before/after is 8 attempts / 20 fits; dedicated E2E enforces unchanged research counts. |
| Refresh / fresh context? | Both pass in the new dedicated Chromium run. New tab in the manual probe is not mislabelled as a new browser context. |
| Review approve/reject? | Earlier actual-page workflow passed with explicitly assistant-authored review fixtures; current automated action/workspace tests pass. Not claimed as a new manual run. |
| Complete Campaign crash/resume? | Earlier real-SPY Windows run passed (24 charged fits, 59.45s); current runtime regression passes. Existing POSIX-specific test remains skipped on Windows. |
| Real BYO user? | No, `BLOCKED_NO_REAL_USER`; CSV/Parquet simulations are engineering only. |
| Genuine independent confirmation? | No, `BLOCKED_NO_ELIGIBLE_DATA`; exposed SPY not reused as blind confirmation. |
| Fresh-process ModelBundle? | Yes again using new browser-generated registered bytes; no registry copy/portable-trust claim. |
| Four strategies use same Controller? | Yes; original `run_benchmark_arm` and persisted contracts/actual target rows independently audited, provider differences explicitly separate. |
| Real LLM benchmark? | Yes; post-quota matrix is attempted under the approved budget and rotations; failed proposals preserved. Exact final arm/HTTP totals are reported separately. |
| Memory cold/warm? | Yes, new actual live two-round arms, frozen accepted-experiment snapshot, equal budget, measured repeats/configs/fits/invalids/metrics. |
| Windows? | New cumulative + actual Chromium + delivery checks; native symlink permission remains BLOCKED_ENV. |
| Linux? | Previous exact `5040c6126373d436d1cc8db90f1318e3c62b0229` core/Chromium/final-SPY CI passed. This is not same-SHA evidence for the new production prompt repair. Local Linux environment: BLOCKED_ENV; new remote CI depends on publishing the new change. |
| macOS? | `BLOCKED_ENV`; no environment was available. |
| Full historical/native? | Full suite executed, all three failures itemized. Original missing assets recovered; affected 27-test combined rerun passes in matching PDF environment. Original native/GPU training remains NOT_RUN_COST. |

## V3 readiness — assessment only

The previous source review still applies: ModelBundle has ordered feature columns/reviewed features, bundle/config/model/source identity, dataset fingerprint, training cutoff/as-of and last training-label availability with its stated basis. None of this supplies a full per-prediction prospective contract for `decision_time`, `prediction_created_at`, `input_asof`, target session, label end/availability, pending→matured→evaluated state and revision policy. No Shadow Forecasting code was added.

```text
V2.2-R ENGINEERING ACCEPTANCE: PARTIAL
REAL USER VALIDATION: BLOCKED
INDEPENDENT FINANCIAL EVIDENCE: BLOCKED
INTERNAL PROSPECTIVE RECORDING: NOT_READY
EXTERNAL_PRODUCT_V3: NOT_READY
V3 PRODUCT DEVELOPMENT: NOT_READY
```

Stop after this user-requested validation and report; no automatic follow-on search or V3 development.
